Ignite, the scalable Perl Socket.IO server

Requirements:
    Mojolicious (http://mojolicio.us/) (get the git version)
    CouchDB (http://couch.io/) (v1.0.0+)

License: MIT

------------------------------------------------

Currently in heavy development, don't expect it to work yet.

The other transports are in the works
